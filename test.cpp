#include <array>
#include <chrono>
#include <iostream>
#include <vector>

int main()
{
    // int num = 10000000;
    // long long x = 5;
    // long long y = 5;
    // char x2 = 5;
    // char y2 = 5;
    // // std::vector<int> vec1(num);
    // // std::vector<std::array<int, 2>> vec2;
    // // std::array<int, 2> arr = {{4, 4}};
    

    // auto t1 = std::chrono::high_resolution_clock::now();
    // for (int i = 0; i < num; i++)
    // {
    //     // vec1.push_back(8);
    //     x == y;
    // }
    // auto t2 = std::chrono::high_resolution_clock::now();
    // std::chrono::duration<double, std::milli> time = t2 - t1;
    // std::cout << time.count() << std::endl;

    // t1 = std::chrono::high_resolution_clock::now();
    // for (int i = 0; i < num; i++)
    // {
    //     // vec2.push_back({4, 4});
    //     x2 == y2;
    // }
    // t2 = std::chrono::high_resolution_clock::now();
    // time = t2 - t1;
    // std::cout << time.count() << std::endl;
    std::cout << (-23 / 8 - 1) << std::endl;
}